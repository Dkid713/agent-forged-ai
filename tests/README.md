# Tests

Add automated tests here. Suggested conventions:

- Mirror the `src/` structure for unit tests (e.g., `tests/server/...`).
- Use a runner such as `vitest` or `jest` if you add suites; include setup in `package.json` scripts.
- Keep fixtures and snapshots alongside the tests that use them.

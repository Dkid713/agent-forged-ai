{
  "name": "agent-forged-ai",
  "version": "0.1.0",
  "description": "Athena / CruxAGI compression and evolution engines",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
